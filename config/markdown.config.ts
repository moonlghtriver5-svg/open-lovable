// Easy toggle for markdown rendering feature
// Set to false to revert to plain text rendering if there are issues
export const MARKDOWN_CONFIG = {
  enabled: false,
  
  // If you need to quickly disable markdown rendering, just set enabled to false:
  // enabled: false,
  
  // This will make all MarkdownRenderer components fall back to plain text
};

export default MARKDOWN_CONFIG;